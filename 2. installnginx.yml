---
- name: Installing ngix server in remotenode.
  hosts: remotenode
  become: yes
  tasks:
    - name: Install nginx
      ansible.builtin.dnf:
        name: nginx
        state: installed

    - name: Start the nginx servive
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes
...